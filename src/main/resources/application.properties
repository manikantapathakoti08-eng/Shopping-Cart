# PostgreSQL Database Connection
spring.datasource.url=jdbc:postgresql:postgresql://neondb_owner:npg_MwxtDiokO7b1@ep-soft-butterfly-axca0zvb-pooler.c-4.us-east-2.aws.neon.tech/neondb?sslmode=require&channel_binding=require
spring.datasource.username=postgres
spring.datasource.password=Mani@062004
spring.datasource.driver-class-name=org.postgresql.Driver

# Flyway Configuration
spring.flyway.enabled=true
spring.flyway.baseline-on-migrate=true

# Hibernate Settings (Validates schema instead of auto-creating it, letting Flyway do the work)
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true


# Tells Spring Boot to use the Cloud Provider's port, or default to 8080 locally
server.port=${PORT:8080}